# vibrantkate
# Developing
The site is simple static resources under the `/docs` folder.

Common commands are documented and automated via a justfile.

If you are developing on Windows, there is a PowerShell script for starting up the dev server.

`./run.ps1`